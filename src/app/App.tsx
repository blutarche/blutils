/**
 * App shell composition root.
 *
 * Phase 1: placeholder while tokens, fonts, chrome, and workspace land
 * in subsequent commits. The grid layout, CSS imports, and component
 * composition arrive when their dependencies are in place.
 */
export function App() {
  return <div>blutils</div>
}
