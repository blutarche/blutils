/**
 * format.json — placeholder until Phase 6.
 *
 * Exists to prove the plugin contract end-to-end before any
 * Tool with real behaviour ships. The body here will be
 * replaced with the actual JSON formatter (validate, indent
 * variants, sort keys, stats) when Phase 6 lands.
 */
export default function Tool() {
  return (
    <div>
      <h1>json.format</h1>
      <p>placeholder — the JSON formatter ships in phase 6.</p>
    </div>
  )
}
